{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Allow the main window to open native directory and export-file pickers.",
  "windows": ["main"],
  "permissions": [
    "core:path:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
