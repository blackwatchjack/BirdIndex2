{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Allow the main window to open a native directory picker.",
  "windows": ["main"],
  "permissions": ["core:path:default", "dialog:allow-open"]
}
