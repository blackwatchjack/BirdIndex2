# BirdIndex2 需求文档：导出物种清单

## 0. 文档信息

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.1 |
| 状态 | 已实现，待桌面端人工验收 |
| 更新日期 | 2026-08-02 |
| 本期范围 | 在扫描摘要区新增“导出物种清单”按钮，并导出当前扫描结果中的物种级 Excel 清单 |
| 目标平台 | macOS、Windows 桌面端（Tauri） |

> 本文同时作为实现和验收基准。自动化验证结果见第 11 节。

## 1. 项目背景

BirdIndex2 是一款本地离线鸟类照片虚拟索引工具。当前桌面端读取随应用打包的 `Multiling IOC 15.1_d.xlsx`，根据照片文件名匹配 IOC 中文名或拉丁学名，并生成 `Order > Family > Genus > Species` 四级分类树。应用不移动、改名或删除原始照片。

用户完成扫描后，目前只能在应用内查看物种和照片，无法把本次扫描发现的物种整理成可留档、分享或进一步统计的清单。因此需要在截图标注的扫描结果区域增加导出入口。

## 2. 目标与成功标准

### 2.1 产品目标

- 用户可在一次有效扫描完成后，从扫描摘要区直接导出 `.xlsx` 物种清单。
- 导出结果以“物种”为粒度：同一物种无论匹配多少张照片，只占一行。
- 清单包含完整分类信息、中文名、拉丁名和匹配照片数，可直接在 Excel 中筛选和排序。
- 导出完全离线，不修改任何原始照片。

### 2.2 成功标准

- 导出的物种行数等于界面中的“命中物种”数。
- 每一行的照片数之和等于界面中的“命中文件”数。
- 用户取消保存时不创建文件、不显示错误。
- 保存失败时不遗留损坏的目标文件或临时文件。
- 包含中文、空格、`&`、`<`、`>` 等字符的名称和路径不会导致工作簿损坏。

## 3. 现有系统基线

本需求以当前代码行为为基础：

- 前端使用 React 18 + TypeScript，桌面容器为 Tauri 2，核心扫描与导出逻辑使用 Rust。
- IOC 数据读取 `List` 工作表中的 `Order`、`Family`、`IOC_15.1`、`Chinese` 字段；Genus 由拉丁学名首词派生。
- 扫描目录支持多选并递归遍历，不跟随符号链接。
- 当前支持 `JPG/JPEG/PNG/HEIC`，扩展名不区分大小写；本需求中的“扫描文件”均指这些受支持的图片文件。
- 文件名匹配不区分拉丁文字大小写，优先匹配拉丁学名，未命中后匹配中文名；单个文件只归入一个物种。
- 扫描结果已包含分类树、每个物种的照片集合、扫描文件数、命中文件数、命中物种数、未命中文件数和 IOC 总物种数，可直接作为导出数据源，无需再次遍历硬盘。
- 当前桌面后端总是解析安装包资源或开发目录中的 IOC 文件；前端可编辑的“IOC 文件路径”尚未进入 Rust `ScanRequest`，不能把该输入框文本当作本次扫描的真实数据源。
- 应用扫描时会短暂写入本地缓存，并在应用启动和退出时清理；“只读”特指不修改用户的原始照片，不代表应用完全没有文件写入权限。

## 4. 名词与数据口径

| 名词 | 定义 |
|---|---|
| 当前扫描结果 | 最近一次成功完成、且扫描参数此后未变化的扫描快照 |
| 扫描文件数 | 本次遍历到的受支持图片文件数量 |
| 命中文件数 | 文件名成功匹配某一 IOC 物种的图片数量 |
| 未命中文件数 | `扫描文件数 - 命中文件数` |
| 命中物种数 | 至少命中一张照片的去重 IOC 物种数量 |
| IOC 总物种数 | 本次扫描所用 IOC 数据库中成功解析的物种数量 |
| 物种清单 | 每个命中物种一行的汇总表，不是每张照片一行的照片明细表 |
| 照片数 | 当前扫描结果中归属于该物种的命中文件数量 |

若用户选择了相互重叠的扫描目录，同一规范化文件路径只能计数一次，避免命中文件数和物种照片数重复累计。

## 5. 用户故事

作为鸟类摄影师或科研人员，我希望在照片扫描完成后，将本次发现的鸟种按 IOC 分类体系导出为 Excel，以便留档、分享和后续统计，而不需要手工抄录分类树。

## 6. 功能需求

### 6.1 导出入口与位置

- 在截图标注的区域，即顶部设置面板第三列的扫描摘要区右下方，放置“导出物种清单”按钮。
- 按钮与扫描统计属于同一视觉分组；不在顶部全局工具栏重复增加入口。
- 常规窗口宽度下按钮右对齐；窄窗口响应式换行后仍应完整显示，不得遮挡统计或反馈信息。
- 按钮使用现有 `ghost` 风格，并具有清晰的 hover、focus、disabled 状态。

### 6.2 按钮状态

| 场景 | 按钮状态 | 文案/反馈 |
|---|---|---|
| 尚未成功扫描 | 禁用 | 文案“导出物种清单”；可通过 `title` 或辅助说明提示“请先完成扫描” |
| 正在扫描 | 禁用 | 保留“导出物种清单” |
| 扫描成功，包含或不包含命中物种 | 启用 | 文案“导出物种清单” |
| 已打开保存窗口但尚未确认 | 防止重复触发 | 不允许同时打开多个保存窗口 |
| 正在写入文件 | 禁用 | 文案“正在导出…” |
| 导出成功 | 恢复启用 | 显示“已导出 N 个物种：完整路径” |
| 导出失败 | 恢复启用 | 显示“导出失败：可理解的原因” |

### 6.3 扫描结果有效性

- 导出使用最近一次成功扫描的完整结果，不受分类树搜索词、展开状态、当前选中物种或当前选中照片影响。
- 用户新增、移除或清空扫描目录后，旧扫描结果立即标记为失效，导出按钮禁用，直到重新扫描成功。
- IOC 数据源必须由扫描响应携带或由后端确认，不得在导出时直接读取可能与扫描无关的输入框文本。若产品继续允许用户切换 IOC 文件，则切换后旧扫描结果立即失效；若本期仍固定使用内置 IOC，则该路径在界面中应只读展示。
- 新扫描开始时清除上一次导出成功或失败反馈；若新扫描失败，不得继续导出旧结果。
- 零命中属于有效扫描结果，允许导出摘要和只有标题行的空物种清单。

### 6.4 保存交互

- 点击按钮后打开系统原生保存窗口，文件类型限定为 Excel 工作簿（`.xlsx`）。
- 默认文件名：`BirdIndex2-物种清单-YYYYMMDD-HHmm.xlsx`，时间使用本机当前时区。
- 用户未输入扩展名时自动补全 `.xlsx`；扩展名大小写不敏感。
- 目标文件已存在时，由系统保存窗口向用户确认是否覆盖。
- 用户取消保存窗口后结束流程：不创建文件、不显示成功或失败反馈。
- 只有用户确认目标路径后才进入“正在导出…”状态。

### 6.5 工作簿结构

导出的工作簿包含两个工作表，顺序固定如下。

#### 6.5.1 `扫描摘要`

| 字段 | 说明 |
|---|---|
| 导出时间 | 本机时间，格式 `YYYY-MM-DD HH:mm:ss` |
| IOC 数据源 | 后端确认的、本次扫描实际使用的 IOC 文件名或路径 |
| 扫描目录数 | 本次有效扫描包含的根目录数量 |
| 扫描目录 | 每个根目录单独一行，保留完整路径 |
| 扫描文件数 | 见第 4 节口径 |
| 命中文件数 | 见第 4 节口径 |
| 未命中文件数 | 见第 4 节口径 |
| 命中物种数 | 见第 4 节口径 |
| IOC 总物种数 | 见第 4 节口径 |

摘要页需注明：“清单基于导出时的扫描快照生成；原始照片未被修改。”

#### 6.5.2 `物种清单`

| 列序 | 字段 | 数据规则 |
|---:|---|---|
| 1 | 序号 | 从 1 连续递增 |
| 2 | 目 Order | IOC `Order`，仅拉丁文 |
| 3 | 科 Family | IOC `Family`，仅拉丁文 |
| 4 | 属 Genus | 拉丁学名首词 |
| 5 | 中文名 | IOC `Chinese`；源数据为空时允许空值 |
| 6 | 拉丁名 | IOC `IOC_15.1` |
| 7 | 照片数 | 当前扫描中该物种的命中文件数 |

- 每个物种只出现一次，以拉丁名对应的 IOC 条目作为去重身份。
- 排序顺序固定为 `Order > Family > Genus > 拉丁名` 升序，与分类树输出保持一致。
- 表头冻结，所有数据列开启自动筛选。
- 设置适合中文名、拉丁名和分类字段的列宽；数字列右对齐或居中。
- 零命中时保留表头，可在第二行显示“本次扫描没有命中的物种”，但该提示不计入数据行数。

### 6.6 成功与失败处理

- 导出成功后返回目标文件完整路径和导出的物种数；前端在扫描面板下方的 `aria-live="polite"` 区域显示反馈。
- 导出先写入同目录临时文件，完整写入并关闭后再替换目标文件，降低中途失败导致文件损坏的风险。
- 失败时删除本次创建的临时文件；未经用户确认不得删除其他文件。
- 常见错误需转为中文可读信息，包括：路径为空、目录不存在、无写入权限、目标文件正被占用、磁盘空间不足、工作簿写入失败。
- 导出期间重复点击、重复调用或并发写入同一目标必须被阻止。

### 6.7 数据安全与隐私

- 所有扫描、聚合和导出均在本地完成，不上传 IOC 数据、照片、文件名或路径。
- 导出只写入用户确认的 `.xlsx` 和必要的同目录临时文件，不移动、重命名、删除或改写原始照片。
- Excel 单工作表最多 1,048,576 行（含标题）；超过上限时在写文件前终止并提示。按当前“每物种一行”设计通常不会触及该限制，但仍需防御性校验。

## 7. 非功能性需求

- **性能**：导出 20,000 个物种应在常规本地磁盘上 5 秒内完成；导出过程不得再次扫描照片目录。
- **响应性**：写入过程不得造成按钮状态失真或允许重复操作；若后端工作会阻塞 UI，需要异步执行。
- **兼容性**：生成文件可被当前主流版本 Microsoft Excel、Apple Numbers 和 LibreOffice Calc 打开。
- **可访问性**：按钮支持键盘焦点和 Enter/Space 触发；状态变化通过 `aria-live` 通知辅助技术。
- **一致性**：界面、默认文件名、工作表名和成功反馈统一使用“物种清单”，不混用“照片清单”或仅“清单”。

## 8. 非本期范围

- CSV、PDF 或其他导出格式。
- 每张照片一行的照片明细，以及文件名、完整照片路径等字段。
- 只导出当前选中物种、搜索结果或某一分类分支。
- 导出未命中文件列表。
- 导出后自动打开 Excel 或自动定位到导出文件。
- 定时导出、历史版本管理、云同步和网络分享。

如后续需要照片级审计，应以独立的“导出照片明细”能力设计，避免改变本功能的物种级数据口径。

## 9. 验收标准

| 编号 | 前置条件与操作 | 预期结果 |
|---|---|---|
| AC-01 | 首次启动，未扫描 | 截图标注位置显示禁用的“导出物种清单”按钮 |
| AC-02 | 扫描成功，命中 3 个物种、10 张照片 | 按钮启用；界面显示命中物种 3、命中文件 10 |
| AC-03 | 点击导出并确认路径 | 默认文件名符合规则，只允许保存为 `.xlsx` |
| AC-04 | 打开导出文件 | 工作表依次为“扫描摘要”“物种清单” |
| AC-05 | 检查物种清单 | 恰好 3 个数据行；同一物种不会因多张照片重复出现；照片数合计为 10 |
| AC-06 | 清单中含中文、`&`、`<`、`>` | 工作簿可正常打开，字符显示正确 |
| AC-07 | 扫描成功但零命中后导出 | 摘要数据正确；物种清单保留表头且无物种数据行；成功提示为 0 个物种 |
| AC-08 | 在保存窗口点击取消 | 不创建文件，不出现成功或错误提示，按钮恢复可用 |
| AC-09 | 导出到无写权限目录或被占用文件 | 显示可理解的失败原因，不留下损坏目标文件或临时文件 |
| AC-10 | 导出过程中连续点击按钮 | 只执行一次导出，不出现多个保存窗口或并发写入 |
| AC-11 | 扫描后修改扫描目录；或在支持切换 IOC 时修改 IOC 路径 | 旧结果失效，按钮禁用，重新扫描成功后才可导出 |
| AC-12 | 分类树输入搜索词或选择某个物种后导出 | 仍导出本次扫描的全部命中物种 |
| AC-13 | 选择父目录和其子目录后扫描 | 同一照片只计一次，导出照片数与界面统计一致 |
| AC-14 | 导出前后比较原始照片 | 文件内容、名称、路径和修改时间均未改变 |

## 10. 实现影响范围

| 模块 | 预期职责 |
|---|---|
| `src/App.tsx` | 按钮、状态机、保存窗口、扫描结果失效逻辑、成功/失败反馈、导出请求字段 |
| `src/styles.css` | 扫描摘要区布局、按钮和反馈状态、窄窗口适配 |
| `src-tauri/src/core/types.rs` | 物种清单导出请求/响应数据结构，响应计数口径为物种数 |
| `src-tauri/src/core/exporter.rs` | 按物种聚合并生成两个工作表、XML 转义、临时文件和原子替换 |
| `src-tauri/src/main.rs`、`core/mod.rs` | 注册并暴露导出命令 |
| `src-tauri/capabilities/main.json` | 允许系统保存窗口 |
| `src-tauri/Cargo.toml` | XLSX 写入所需依赖 |
| `README.md` | 用户流程和导出字段说明与本需求保持一致 |

建议后端导出请求携带扫描快照对应的扫描目录和 IOC 数据源，避免读取导出瞬间可能已经变化的前端输入状态。

## 11. 实现与自动化验证状态

当前实现已完成以下内容：

1. 按钮、系统保存窗口、默认文件名、工作表名和反馈文案已统一为“物种清单”。
2. 导出结果按物种聚合，每个物种一行，返回值为 `species_count`。
3. 摘要页包含导出时间、后端确认的 IOC 数据源、扫描目录和完整统计。
4. 扫描响应携带扫描快照；目录变化会清除旧结果并禁用导出。
5. 内置 IOC 路径在界面中只读展示，不再向后端发送无效的 IOC 或缓存路径字段。
6. 扫描根目录会先规范化并移除父子重叠目录，结果聚合阶段再按路径防御性去重。
7. 导出使用同目录临时文件；覆盖已有文件时先保留备份，写入失败会恢复原文件。
8. Rust 自动化测试覆盖物种级导出、零命中、中文与特殊字符、摘要字段和重叠目录去重。
9. 前端生产构建、Rust 测试和 Rust 格式检查已通过。

仍需在真实桌面应用中人工验收系统保存窗口、覆盖确认、无写权限路径，以及用 Excel、Numbers、LibreOffice 打开生成文件的兼容性。

## 12. 发布前检查

- 前端 TypeScript 构建通过。
- Rust 单元测试和格式检查通过。
- 至少在 macOS、Windows 各进行一次真实保存窗口与 Excel 打开测试。
- 使用包含中文目录、中文文件名、特殊字符、零命中、重复目录和无写权限路径的样例验证。
- README、按钮文案、工作表名称和成功提示均与本文一致。
